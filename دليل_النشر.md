# 🚀 دليل نشر ZONE X (التطبيق + الموقع)

في **شيئين منفصلين**:
1. **التطبيق المحلي** → بينحط على كمبيوتر كل محل (تطبيق سطح مكتب، أوفلاين).
2. **الموقع (Portal)** → بينرفع على سيرفر إنترنت، بيشتغل ٢٤/٧ بدون لابتوبك.

---

## ① تثبيت التطبيق عند صاحب المحل (Desktop App)

التطبيق بينعمل ملف تثبيت `.exe` — صاحب المحل بس بيعمل تنصيب ودبل كليك. **ما بدو يثبّت بايثون ولا أي شي.**

### خطوات بناء ملف التثبيت (تعملها إنت مرة):
1. ثبّت **Inno Setup** (برنامج مجاني لصنع ملفات التثبيت): https://jrsoftware.org/isdl.php
2. من مجلّد `CafeCloud` شغّل:
   ```
   build_exe.bat
   ```
   (هاد بيصنع نسخة التطبيق داخل مجلّد `dist\ZONE X`)
3. افتح ملف `installer.iss` ببرنامج Inno Setup واضغط **Compile** (أو F9).
4. بينطلعلك ملف التثبيت بمجلّد `installer_output\ZONE_X_Setup.exe`.

### عند صاحب المحل:
- تعطيه ملف `ZONE_X_Setup.exe` (USB أو تحميل).
- بيعمل تنصيب → بيصير في **أيقونة ZONE X على سطح المكتب**.
- دبل كليك → بيفتح متل أي برنامج، أول مرة بيطلب **اسم المحل + كود التفعيل**.
- البيانات كلها بتنحفظ على جهازه (SQLite) — يشتغل بدون إنترنت.

⚠️ **قبل ما تبيع:** غيّر `LICENSE_SECRET` في `cafe/models.py` لشي سري خاص فيك (ولازم نفسه بالموقع).

---

## ② نشر الموقع ٢٤/٧ (Portal)

عشان الموقع يضل شغّال بدون لابتوبك، بدو يكون على **سيرفر إنترنت**.

### المطلوب:
| البند | من وين | التكلفة |
|---|---|---|
| دومين (مثل zonex.com) | Namecheap / GoDaddy | ~12$/سنة |
| استضافة | PythonAnywhere (الأسهل) أو VPS | 5–6$/شهر |
| قاعدة بيانات | مع الاستضافة | مجاناً |
| HTTPS (قفل أخضر) | Let's Encrypt | مجاناً |

### الطريقة الأسهل للمبتدئ — PythonAnywhere:
1. سجّل حساب على https://www.pythonanywhere.com
2. ارفع مجلّد `ZoneX-Portal` (عبر Git أو رفع ملفات).
3. من تبويب **Web** → Add a new web app → Manual config (Python 3.11+).
4. اعمل Virtualenv وثبّت: `pip install -r requirements.txt`
5. اربط الإعدادات:
   - WSGI file → يشير لـ `portal_core.wsgi`
   - Static files → `/static/` و `/media/`
6. شغّل: `python manage.py migrate` و `python manage.py createsuperuser`
7. اربط الدومين من تبويب Web → Add domain.

### الطريقة الاحترافية — VPS (Hetzner/Contabo):
1. اشترِ VPS صغير (Ubuntu) ~5$/شهر.
2. ثبّت: Python + PostgreSQL + Nginx + Gunicorn.
3. ارفع الكود، `pip install -r requirements.txt`.
4. حوّل قاعدة البيانات لـ PostgreSQL (عدّل `DATABASES` في `settings.py`).
5. شغّل Gunicorn خلف Nginx، وفعّل SSL بـ Certbot.
6. وجّه الدومين (DNS) لـ IP السيرفر.

### ⚠️ تعديلات إلزامية قبل النشر (في `portal_core/settings.py`):
```python
DEBUG = False
ALLOWED_HOSTS = ['zonex.com', 'www.zonex.com']   # دومينك
SECRET_KEY = '...'                                # غيّره لمفتاح سري طويل
LICENSE_SECRET = '...'                            # نفسه الموجود بالتطبيق المحلي
```

---

## ③ الربط بين الاثنين 🔑
- **`LICENSE_SECRET` لازم يكون نفسه** في:
  - `CafeCloud/cafe/models.py`
  - `ZoneX-Portal/portal_core/settings.py`
- هيك الكود اللي بيتولّد من الموقع بيُقبل بالتطبيق المحلي.

---

## باختصار سير العمل الكامل:
1. زبون يدخل موقعك → يسجّل محلّه → يدفع → يرفع إيصال.
2. إنت من لوحة الإدارة `/manage/` توافق → يتولّد كود.
3. الزبون يحمّل `ZONE_X_Setup.exe` ويثبّته → يدخل اسم محلّه + الكود → يشتغل.
